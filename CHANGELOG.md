# 업데이트 내역

이 프로젝트는 날짜 기반 버전(`vYYYY.MM.DD`)을 사용합니다.

## v2026.04.24 — 2026-04-24

### 추가

- 관리자 기본 편집 흐름을 위한 **퀵스타트 마법사**를 추가했습니다.
- 퀵스타트에서 사이트 정보, 히어로, 경력/툴, 가격, 문의, JSON, 임베드 카드, 대표 이미지 작업을 순서대로 진행할 수 있게 했습니다.
- 링크 미리보기용 **임베드 카드 HTML 생성/복사** 흐름을 강화했습니다.
- 소셜 대표 이미지 `social-preview.png` 제작을 위한 1200×630 이미지 자르기 흐름을 추가했습니다.
- `projects.youtubeChannel` 데이터 구조를 추가해 유튜브 채널 카드 정보를 관리할 수 있게 했습니다.
- 영상 포트폴리오 표시 방식에 카테고리 스택과 그리드를 함께 쓰는 `hybrid` 모드를 추가했습니다.
- 관리자 구조 회귀 방지를 위한 `scripts/validate-admin-quickstart.mjs` 검증 스크립트를 추가했습니다.
- 현재 버전을 기록하는 `VERSION` 파일을 추가했습니다.

### 개선

- 모바일 관리자 화면에서 탭 목록을 가로 스크롤 대신 접이식 메뉴처럼 사용할 수 있게 개선했습니다.
- 퀵스타트의 가격 플랜 편집 UI를 접이식 카드 형태로 정리했습니다.
- 가격 플랜 내부의 `위로`, `아래로`, `복사`, `삭제` 버튼이 과도하게 커지지 않도록 스타일을 보정했습니다.
- 강조 키워드 처리를 개선해 쉼표, `#`, 줄바꿈으로 나눈 여러 키워드를 제목 안에서 강조할 수 있게 했습니다.
- 관리자와 공개 페이지의 캐시 버스터 쿼리 문자열을 `20260424` 기준으로 정리했습니다.
- 공개 페이지 내비게이션에서 `index.html#home`, `index.html#works` 같은 홈 섹션 링크의 활성 표시, 페이지 간 이동 후 스크롤 위치, active underline 정렬을 보정했습니다.
- `data/example.site.json`을 최신 데이터 구조에 맞게 갱신했습니다.

### 변경

- `data/site.json`은 계속 로컬/운영 데이터로 취급하며 기본적으로 git 추적 대상에서 제외합니다.
- README를 최신 관리자 흐름, 로컬 실행법, 링크 미리보기 수정법, 데이터 정책, 버전 관리 방식 중심으로 다시 작성했습니다.

### 검증

- `node scripts/validate-admin-quickstart.mjs`
- `node --check admin/admin.js`
- `node --check scripts/validate-admin-quickstart.mjs`
- `python3 -m json.tool data/example.site.json`
- `git diff --check`
